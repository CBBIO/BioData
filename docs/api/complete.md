# Complete public API

```python
import CBBIO

print(len(CBBIO.__all__))
```

This page renders every symbol exported from the public `CBBIO` namespace. Prefer the subsystem pages for normal use.

## Public symbols

::: CBBIO
    options:
      members: true
      show_root_members_full_path: false

## Exceptions

| Exception | When raised |
|---|---|
| `BioDataError` | BioData client operation fails |
| `EmbeddingGenerationError` | Embedding generation fails |
| `SearchIndexError` | Persistent index operation fails |
